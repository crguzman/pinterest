<div class="container">

<%= link_to 'New Pin', new_pin_path, :class => 'btn btn-primary', :style => 'color:white' %>

  <h1>Listing pins</h1>


  <% @pins.each do |pin| %>
    <div class="col-md-3">
      <div class="thumbnail">
        <%= link_to(image_tag(pin.photo, :class => 'img-responsive'),pin) %>
        <%= pin.description %>
        <br>
        <%= link_to 'Editar', edit_pin_path(pin), :class => 'btn btn-primary', :style => 'color:white' %>
        <%= link_to 'Borrar', pin, method: :delete, data: { confirm: 'Estás seguro?' }, :class => 'btn btn-danger', :style => 'color:white'  %>
      </div>
    </div>
   <% end %>

  </div>